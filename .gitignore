.venv/
data/twcs/
.env
__pycache__/
*.pyc

# the assignment brief itself -- not part of the deliverable
Hiver SDE Intern Assignment.docx

# ad-hoc backups
*.bak

# markdown docs -- kept local, not tracked. README.md is the exception:
# it is the repo's single document and carries the full report.
*.md
!README.md

# local Claude Code config
.claude/

# Chroma index -- 17MB of rebuildable binary; querying needs the MiniLM
# model anyway, so committing it would not remove the download step.
# Rebuild: scripts/vector_retrieval.py --build
data/chroma/
